import { LoginForm } from "@/domain/user";
import { apiClient } from "@/utils/client";

// export const loginUser = async (data: LoginForm) => {
// 	const response = await apiClient.post("/user/signin", data);
// 	return response.data;
// };
