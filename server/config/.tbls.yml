# .tbls.yml
# DSN (Database Source Name) to connect database
dsn: mysql://test_user:test_db@127.0.0.1:3306/db
# Path to generate document
# Default is `dbdoc`
docPath: docs/schema

disableOutputSchema: true

er:
  format: mermaid